import styles from "./style.module.css"


const Repo = ({userName}) => {
  return (
    <ul>
      {/* {
      userRepos?.map((userRepo)=><li>{userRepo.stargazers_count}</li>)
      } */}
      </ul>
  )
}

export default Repo